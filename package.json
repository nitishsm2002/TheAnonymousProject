{
  "name": "the-anonymous-project",
  "version": "1.0.0",
  "description": "The Anonymous Project - SAP BTP-native solution",
  "scripts": {
    "start": "cds run",
    "watch": "cds watch",
    "build": "cds build/all",
    "deploy:cf": "mbt build && cf deploy mta_archives/*.mtar"
  },
  "dependencies": {
    "@sap/cds": "^9",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9",
    "mbt": "^1"
  },
  "private": true
}
