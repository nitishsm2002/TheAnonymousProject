{
  "name": "the-anonymous-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@sap/cds": "^9.5.1",
    "@cap-js/sqlite": "^2.1.0",
    "express": "^4"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "sqlite.db"
        }
      }
    }
  }
}
